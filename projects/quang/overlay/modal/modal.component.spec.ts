import { QuangModalComponent } from './modal.component'

describe('QuangModalComponent', () => {
  it('should be defined', () => {
    expect(QuangModalComponent).toBeDefined()
  })

  // TODO: Add tests with required inputs (position)
})
