import { QuangTooltipComponent } from './tooltip.component'

describe('QuangTooltipComponent', () => {
  it('should be defined', () => {
    expect(QuangTooltipComponent).toBeDefined()
  })

  // TODO: Add tests with required inputs (overlayContent)
})
