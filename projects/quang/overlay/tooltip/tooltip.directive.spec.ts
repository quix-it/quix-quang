import { QuangTooltipDirective } from './tooltip.directive'

describe('QuangTooltipDirective', () => {
  it('should be defined', () => {
    expect(QuangTooltipDirective).toBeDefined()
  })

  // TODO: Add tests with Overlay and ElementRef mocks
})
