import { IsNotAuthenticatedDirective } from './is-not-authenticated.directive'

describe('IsNotAuthenticatedDirective', () => {
  it('should create an instance', () => {
    const directive = new IsNotAuthenticatedDirective()
    expect(directive).toBeTruthy()
  })
})
