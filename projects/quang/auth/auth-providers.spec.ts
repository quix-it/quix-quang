import { provideAuth } from './auth-providers'

describe('Auth Providers', () => {
  it('provideAuth should be defined', () => {
    expect(provideAuth).toBeDefined()
  })

  // TODO: Add tests for provider configuration
})
