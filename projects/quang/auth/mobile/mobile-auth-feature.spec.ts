import { withMobileAuth } from './mobile-auth-feature'

describe('MobileAuthFeature', () => {
  it('withMobileAuth should be defined', () => {
    expect(withMobileAuth).toBeDefined()
  })

  // TODO: Add tests for mobile auth feature
})
