import { withSessionStorage } from './session-storage-feature'

describe('SessionStorageFeature', () => {
  it('withSessionStorage should be defined', () => {
    expect(withSessionStorage).toBeDefined()
  })

  // TODO: Add tests for sessionStorage feature
})
