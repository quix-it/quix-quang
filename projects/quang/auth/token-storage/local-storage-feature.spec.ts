import { withLocalStorage } from './local-storage-feature'

describe('LocalStorageFeature', () => {
  it('withLocalStorage should be defined', () => {
    expect(withLocalStorage).toBeDefined()
  })

  // TODO: Add tests for localStorage feature
})
