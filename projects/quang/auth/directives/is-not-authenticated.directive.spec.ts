import { QuangIsNotAuthenticatedDirective } from './is-not-authenticated.directive'

describe('QuangIsNotAuthenticatedDirective', () => {
  it('should be defined', () => {
    expect(QuangIsNotAuthenticatedDirective).toBeDefined()
  })

  // TODO: Add tests with QuangAuthService mock
})
