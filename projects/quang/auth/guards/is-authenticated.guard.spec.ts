import { quangIsAuthenticatedGuard } from './is-authenticated.guard'

describe('IsAuthenticatedGuard', () => {
  it('should be defined', () => {
    expect(quangIsAuthenticatedGuard).toBeDefined()
  })

  // TODO: Add tests with QuangAuthService mock and Router
})
