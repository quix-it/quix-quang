import { quangIsAllowedGuardFactory } from './is-allowed.guard'

describe('IsAllowedGuard', () => {
  it('should be defined', () => {
    expect(quangIsAllowedGuardFactory).toBeDefined()
  })

  // TODO: Add tests with QuangAuthService mock and Router
})
