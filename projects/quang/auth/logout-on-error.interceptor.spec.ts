import { logoutOnErrorInterceptor } from './logout-on-error.interceptor'

describe('LogoutOnErrorInterceptor', () => {
  it('should be defined', () => {
    expect(logoutOnErrorInterceptor).toBeDefined()
  })

  // TODO: Add tests with HttpTestingController
})
