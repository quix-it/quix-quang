import { QuangAuthService } from './auth.service'

describe('QuangAuthService', () => {
  it('should be defined', () => {
    expect(QuangAuthService).toBeDefined()
  })

  // TODO: Add tests with OAuthService mock
})
