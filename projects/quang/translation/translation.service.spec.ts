import { QuangTranslationService } from './translation.service'

describe('QuangTranslationService', () => {
  it('should be defined', () => {
    expect(QuangTranslationService).toBeDefined()
  })

  // TODO: Add tests with TranslocoService mock
})
