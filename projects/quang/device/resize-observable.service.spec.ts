import { ResizeObservableService } from './resize-observable.service'

describe('ResizeObservableService', () => {
  it('should be defined', () => {
    expect(ResizeObservableService).toBeDefined()
  })

  // TODO: Add tests with ResizeObserver mock
})
