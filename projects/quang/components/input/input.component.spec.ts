import { QuangInputComponent } from './input.component'

describe('QuangInputComponent', () => {
  it('should be defined', () => {
    expect(QuangInputComponent).toBeDefined()
  })

  // TODO: Add tests with required inputs and providers
})
