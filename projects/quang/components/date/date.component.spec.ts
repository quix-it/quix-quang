import { QuangDateComponent } from './date.component'

describe('QuangDateComponent', () => {
  it('should be defined', () => {
    expect(QuangDateComponent).toBeDefined()
  })

  // TODO: Add tests with QuangTranslationService provider
})
