import { QuangTableComponent } from './table.component'

describe('QuangTableComponent', () => {
  it('should be defined', () => {
    expect(QuangTableComponent).toBeDefined()
  })

  // TODO: Add tests with ResizeObservableService
})
