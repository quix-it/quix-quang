import { QuangOptionListComponent } from './option-list.component'

describe('QuangOptionListComponent', () => {
  it('should be defined', () => {
    expect(QuangOptionListComponent).toBeDefined()
  })

  // TODO: Add tests with required inputs (parentType)
})
