import { PaginatorIntl } from './paginatorIntl'

describe('PaginatorIntl', () => {
  it('should be defined', () => {
    expect(PaginatorIntl).toBeDefined()
  })

  // TODO: Add tests for paginator internationalization
})
