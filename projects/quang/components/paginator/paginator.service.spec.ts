import { QuangPaginatorService } from './paginator.service'

describe('QuangPaginatorService', () => {
  it('should be defined', () => {
    expect(QuangPaginatorService).toBeDefined()
  })

  // TODO: Add tests for paginator service
})
