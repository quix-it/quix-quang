import { QuangPaginatorComponent } from './paginator.component'

describe('QuangPaginatorComponent', () => {
  it('should be defined', () => {
    expect(QuangPaginatorComponent).toBeDefined()
  })

  // TODO: Add tests with TranslocoService provider
})
