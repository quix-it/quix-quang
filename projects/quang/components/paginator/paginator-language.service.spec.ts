import { QuangPaginatorLanguageService } from './paginator-language.service'

describe('QuangPaginatorLanguageService', () => {
  it('should be defined', () => {
    expect(QuangPaginatorLanguageService).toBeDefined()
  })

  // TODO: Add tests with TranslocoService mock
})
