import { QuangAuthImageDirective } from './quang-auth-image.directive'

describe('QuangAuthImageDirective', () => {
  it('should create an instance', () => {
    const directive = new QuangAuthImageDirective()
    expect(directive).toBeTruthy()
  })
})
