import { HasStoreRoleDirective } from './has-store-role.directive';

describe('HasStoreRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new HasStoreRoleDirective();
    expect(directive).toBeTruthy();
  });
});
