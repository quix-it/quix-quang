import { HasUntilRolesDirective } from './has-until-roles.directive';

describe('HasUntilRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new HasUntilRolesDirective();
    expect(directive).toBeTruthy();
  });
});
