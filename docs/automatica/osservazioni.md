# Osservazioni sul codice

Righe aggiunte dai run di documentazione automatica. `Ticket: —` significa osservazione in attesa,
non osservazione già segnalata.

| Repo | Punto | Osservazione | Vista a | Ticket |
| --- | --- | --- | --- | --- |
| quix-quang | `projects/quang/index.ts:provideQuangConfig` | il flag `overlayUsePopover` non ha lettori: la chiave `usePopover` che registra su `OVERLAY_DEFAULT_CONFIG` non è letta da nessuna parte nel repo, e il provider sostituisce il valore del token invece di estenderlo | 68c44102 | QUANG-281 |
| quix-quang | `projects/quang/components/table/table.component.ts:_tdWithPropertiesEffect` | scrive chiavi arbitrarie di `cell.properties` su `nativeElement`, aggirando la sanificazione di Angular; il `JSON.parse` dell'attributo `data-properties` non è protetto e gira dentro un `effect()` | 68c44102 | QUANG-282 |
| quix-quang | `projects/quang/auth/directives/is-authenticated.directive.ts:QuangIsAuthenticatedDirective` | il ramo autenticato dell'effect non assegna il risultato di `createEmbeddedView` a `embeddedViewRef`: la guardia resta sempre vera e ogni rivalutazione aggiunge una view senza togliere la precedente. Le tre direttive sorelle assegnano | 68c44102 | QUANG-285 |
| quix-quang | `projects/quang/components/autocomplete/autocomplete.component.ts:handleInputKeydown` | registra un listener `keydown` sull'ultimo chip a ogni Backspace sulla casella, senza rimuoverlo né controllare se c'è già: i listener si sommano sullo stesso bottone e un solo Backspace cancella più chip | 68c44102 | QUANG-286 |
| quix-quang | `projects/quang/auth/auth-providers.ts:withAuth` | si dichiara `QuangFeatureKind.LoaderFeature` invece di `AuthFeature`, che resta l'unico valore dell'enum mai usato. Innocuo a runtime — `ɵkind` non è letto da nessuna parte — ma il tipo di ritorno mente sulla feature | 68c44102 | QUANG-290 |
| quix-quang | `projects/quang/index.ts:QUANG_CONFIG` | il token è dichiarato ed esportato ma non viene registrato da `provideQuangConfig` né iniettato in nessun punto del repo: chi lo inietta non riceve la configurazione | 68c44102 | QUANG-289 |
| quix-quang | `projects/quang/components/date/date.component.ts:setupCalendar` | il callback `onShow` passato da fuori con `datepickerOptions` viene invocato solo quando l'animazione non è completa **e** il timepicker è attivo: le due condizioni prima escono con `return`, quindi nel caso comune non viene chiamato. `onSelect` e `onHide` invece invocano sempre il callback dell'utente | 6252424 | QUANG-293 |
| quix-quang | `projects/quang/components/input/input.component.html:bottone della password` | il bottone che mostra e nasconde la password porta `aria-label="calendar-button"` e il riferimento di template `#calendarButton`, copiati dal componente date: uno screen reader annuncia un bottone del calendario dentro un campo password | 6252424 | QUANG-294 |
| quix-quang | `projects/quang/components/shared/quang-base-component.directive.ts:onChangedEventHandler` | propaga `inputElement.value as T` senza conversione: con `quang-input` e `componentType="number"` il `FormControl` riceve una stringa, benché il componente sia tipato `string \| number`. Il cast nasconde la differenza al compilatore | 6252424 | QUANG-297 |
| quix-quang | `projects/quang/components/paginator/paginator.component.html:36` | l'etichetta della posizione corrente legge l'input `page()`, mentre i quattro bottoni leggono il segnale interno `_currentPage()`: se chi usa il componente non riscrive l'input dopo `changePage`, i bottoni si spostano e il numero mostrato resta indietro | 6252424 | QUANG-298 |
| quix-quang | `projects/quang/components/paginator/paginator-language.service.ts:getAndInitTranslations` | carica cinque chiavi (`quangPaginator.itemsPerPage`, `.nextPage`, `.previousPage`, `.firstPage`, `.lastPage`) che non esistono nei bundle i18n del repo e che nessun template rende; il servizio e la sua base `PaginatorIntl` non sono iniettati da nessuna parte, e il paginator traduce per conto proprio con altre tre chiavi | 6252424 | QUANG-301 |
| quix-quang | `projects/quang/components/paginator/paginator.component.html:17` | il `<select>` della dimensione pagina porta insieme `[id]="componentId() + '-quang-page-size'"` e l'attributo statico `id="pageSize"`: a runtime vince il binding, quindi la `<label for="pageSize">` accanto non è più associata a nessun elemento | 6252424 | QUANG-302 |
| quix-quang | `projects/quang/components/paginator/paginator.component.html:136` | quattro `<ng-template>` in coda al template (`#nextTemplate`, `#prevTemplate`, `#lastTemplate`, `#firstTemplate`) non sono referenziati da nessun punto: sono icone di una versione precedente dei bottoni, e ognuna ripete due volte l'attributo `fill` | 6252424 | — |
| quix-quang | `projects/quang/components/select/select.component.ts:onBlurHandler` | l'override chiama `super.onBlurHandler()` solo in modo `single`: in modo `multiple` il blur non marca il controllo come toccato e non emette `componentBlur`, quindi un utente che entra nel campo e ne esce senza scegliere non vede mai il messaggio di errore, che dipende da `_isTouched()` | 6252424 | — |
| quix-quang | `projects/quang/components/select/select.component.html:32` | `[attr.aria-controls]` vale la stringa `'optionList'`, ma `#optionList` è un riferimento di template e non un `id` del DOM: nessun elemento ha quell'id, quindi il bottone dichiara di controllare un elemento che non esiste | 6252424 | — |
| quix-quang | `projects/quang/components/select/select.component.ts:providers` | il provider `{ provide: QuangOptionListComponent, multi: false }` non ha `useClass`, `useValue`, `useExisting` né `useFactory`: Angular ripiega sul token come classe e istanzia il componente della lista via DI, in aggiunta a quello che il template crea | 6252424 | — |
| quix-quang | `projects/quang/components/table/table.component.html:18` | il blocco che disegna una colonna dell'intestazione (righe 18-53) è ricopiato identico alle righe 72-107 per la seconda intestazione: una modifica al markup dell'ordinamento va fatta due volte, e le due copie devono restare uguali perché il calcolo delle larghezze accoppia le colonne per indice | 6252424 | — |
| quix-quang | `projects/quang/components/table/table.component.ts:104` | `_tableHeader` e `_tableHeaderElement` sono due `viewChild` sullo stesso riferimento `#tableHeader`, con due effect (righe 125 e 131) che chiamano entrambi `fixTableHeaderWidth()`: il secondo non è letto da nessun'altra parte e il metodo viene invocato due volte per lo stesso evento | 6252424 | — |
| quix-quang | `projects/quang/components/table/table.component.ts:118` | traccia di debug lasciata nel codice: un `console.log` commentato dentro `_tdWithPropertiesEffect`, nel ciclo che assegna le proprietà sull'elemento nativo | 6252424 | — |
| quix-quang | `projects/quang/components/tabs/tabs.component.html:1` | la striscia delle linguette non porta nessun attributo di accessibilità: nessun `role="tablist"` sul contenitore, nessun `role="tab"` né `aria-selected` sui bottoni. La linguetta attiva è distinta solo da una classe CSS, quindi chi usa uno screen reader sente una fila di bottoni e non sa quale è quello scelto | 6252424 | — |
| quix-quang | `projects/quang/components/tabs/tabs.component.html:1` | il componente estende la base dei controlli di form ma il suo template non rende etichetta, messaggio di errore, di successo né di aiuto: gli input `componentLabel`, `errorMap`, `successMessage` e `helpMessage`, ereditati e accettati senza segnalazione, non hanno nessun effetto su questo componente | 6252424 | — |
| quix-quang | `projects/quang/components/wysiwyg/wysiwyg.component.ts:149` | due `catch (_)` vuoti — qui e alla riga 184 — inghiottono ogni errore della creazione dell'editor e della scrittura del contenuto. Il commento accanto motiva solo il caso della modal, ma il blocco cattura tutto: se SunEditor fallisce per un altro motivo, il campo resta una `textarea` vuota senza nessuna traccia in console | 6252424 | — |
| quix-quang | `projects/quang/components/wysiwyg/wysiwyg.component.ts:191` | il metodo `validate` ha la firma di un validatore ma il componente non si registra fra gli `NG_VALIDATORS`: viene richiamato solo a mano dentro `onChangedHandler`, quindi un valore iniziale fatto di soli tag HTML non viene mai marcato come mancante finché l'utente non tocca il campo | 6252424 | — |
| quix-quang | `projects/quang/data-handling/conversion.ts:blobToBase64` | il nome promette base64 ma la funzione risolve con quello che `readAsDataURL` produce, cioè un data URI completo `data:<tipo>;base64,<payload>`: passarne il risultato a `base64ToDataUri`, la funzione accanto, produce un prefisso doppio. Il round-trip nello spec (riga 144) salta la funzione in mezzo con un commento che lo dichiara | 6252424 | — |
| quix-quang | `projects/quang/data-handling/download.ts:downloadFile` | `window.URL.revokeObjectURL(url)` viene chiamato sincronamente subito dopo `a.click()`, senza attendere che il browser abbia iniziato a leggere l'URL; l'`<a>` non viene mai appeso al documento | 6252424 | — |
| quix-quang | `projects/quang/data-handling/download.ts:handleDownload` | il ramo di ripiego sul nome del file non è un `if`: l'estrazione è scritta `r.exec(contentDisposition)![1]`, e quando l'header `content-disposition` manca o non corrisponde è il `TypeError` dell'indicizzazione di `null` a portare nel `catch`. Un errore di programmazione dentro lo stesso `try` finirebbe nello stesso ramo, silenziosamente | 6252424 | — |
| quix-quang | `projects/quang/device/resize-observable.service.ts:resizeObservable` | il teardown della sottoscrizione chiama `unobserve(elem)`, che è per elemento e non per sottoscrittore: l'osservatore è uno solo e non conta quante sottoscrizioni riguardino lo stesso elemento, quindi chiudere una delle due smette di osservare per entrambe | 6252424 | — |
| quix-quang | `projects/quang/device/resize-observable.service.ts:resizeObservable` | `observe(elem)` è chiamato all'invocazione del metodo, prima di costruire l'`Observable`, mentre la registrazione fra i notificatori avviene alla sottoscrizione: chiamare il metodo senza sottoscriversi lascia l'elemento osservato senza che esista un teardown da eseguire per smettere | 6252424 | — |
| quix-quang | `projects/quang/device/resize-observable.service.ts:widthResizeObservable` | il `filter(Boolean)` dopo `map` scarta la larghezza 0: quando l'elemento osservato si riduce a larghezza nulla — perché viene nascosto — il valore non viene emesso e il sottoscrittore resta sull'ultima larghezza utile invece di sapere che è collassato | 6252424 | — |
